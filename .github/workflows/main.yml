name: 🕷️ Spidey Persistent VPS - Snapshot Optimized

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

env:
  SNAPSHOT_NAME: vps-snapshot-latest.img
  SNAPSHOT_MOUNT: /mnt/snapshot
  MEGA_REMOTE: mega:vps-backup

jobs:
  vps:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      # 1. Initial Setup
      - name: 👤 Create User & Set Hostname
        run: |
          echo "🚀 Creating universal user and hostname setup..."
          sudo useradd -m -s /bin/bash jacky
          echo "jacky:root" | sudo chpasswd
          sudo usermod -aG sudo jacky
          echo "jacky ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/jacky
          sudo hostnamectl set-hostname github-vps

      # 2. Install Required Tools
      - name: ⚡ Install System Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y curl wget unzip tar psmisc screen openssh-server \
            mariadb-server apache2 php php-mysql qemu-utils cloud-utils
          curl -fsSL https://rclone.org/install.sh | sudo bash
          sudo systemctl enable --now ssh

      # 3. Configure Rclone for MEGA
      - name: ☁️ Configure Rclone (MEGA)
        run: |
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf <<EOF
          [mega]
          type = mega
          user = ${{ secrets.MEGA_USER }}
          pass = ${{ secrets.MEGA_PASS }}
          EOF

      # 4. Snapshot Restoration
      - name: ♻️ Restore from Snapshot
        id: restore
        run: |
          echo "🔍 Checking for existing snapshot on MEGA..."
          if rclone ls ${MEGA_REMOTE}/${SNAPSHOT_NAME}.gz >/dev/null 2>&1; then
            echo "✅ Snapshot found. Restoring system..."
            
            # Download snapshot
            rclone copy ${MEGA_REMOTE}/${SNAPSHOT_NAME}.gz /tmp/
            
            # Decompress
            gunzip /tmp/${SNAPSHOT_NAME}.gz
            
            # Create mount point
            sudo mkdir -p ${SNAPSHOT_MOUNT}
            
            # Mount the snapshot image
            sudo mount -o loop /tmp/${SNAPSHOT_NAME} ${SNAPSHOT_MOUNT}
            
            # Rsync data back to root filesystem
            echo "🔄 Syncing snapshot data to live system..."
            sudo rsync -a ${SNAPSHOT_MOUNT}/ / --exclude=/proc --exclude=/sys --exclude=/dev
            
            # Cleanup
            sudo umount ${SNAPSHOT_MOUNT}
            sudo rm -f /tmp/${SNAPSHOT_NAME}
            
            echo "is_new_install=false" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ No snapshot found. Fresh installation."
            echo "is_new_install=true" >> $GITHUB_OUTPUT
          fi

      # 5. Delete Old Snapshot
      - name: 🗑️ Delete Old Snapshot
        if: steps.restore.outputs.is_new_install == 'false'
        run: |
          echo "Deleting old snapshot from MEGA..."
          rclone delete ${{ env.MEGA_REMOTE }}/${{ env.SNAPSHOT_NAME }}.gz

      # 6. Install and Configure Tailscale
      - name: 🦊 Install & Setup Tailscale
        run: |
          echo "🦊 Installing Tailscale..."
          curl -fsSL https://tailscale.com/install.sh | sh
          
          echo "🔧 Starting Tailscale service..."
          sudo systemctl enable --now tailscaled
          
          echo "🌐 Connecting to Tailscale network..."
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --hostname=github-vps --reset
          
          # Wait for network to stabilize
          echo "⏳ Waiting for Tailscale network to establish..."
          sleep 15
          
          # Display Tailscale IP
          TAILSCALE_IP=$(sudo tailscale ip -4)
          echo "✅ Tailscale IP: $TAILSCALE_IP"

      # 7. System Setup & Services
      - name: 🚀 System Setup
        run: |
          # Create essential directories
          sudo mkdir -p /opt /srv /var/www /var/lib/mysql /www/server /home/jacky
          
          # Install Aapanel (non-interactive)
          curl -sSLo /tmp/install.sh http://www.aapanel.com/script/install-ubuntu_6.0_en.sh
          (echo "y" && echo "yes") | sudo bash /tmp/install.sh

      # 8. Start Services
      - name: 🚀 Start Services
        run: |
          sudo systemctl daemon-reload
          
          # Start services in order
          sudo systemctl start mariadb || true
          sudo bt restart || true
          sudo systemctl start apache2 || true
          
          # Verify Tailscale is still running
          sudo systemctl restart tailscaled || true
          echo "✅ All services started"

      # 9. Configure Aapanel
      - name: 🔐 Set Aapanel Credentials
        run: |
          echo "jacky" | sudo bt 6
          echo "spidey" | sudo bt 5
          echo "✅ Aapanel configured: jacky/spidey"

      # 10. Start Tmate Session
      - name: 🛰️ Start Tmate Session
        run: |
          sudo apt-get install -y tmate
          sudo -u jacky screen -dmS tmate-session tmate
          sleep 3
          echo "✅ Tmate session started"

      # 11. Display Connection Info
      - name: 📊 Display Connection Information
        run: |
          echo "=== 🖥️ VPS CONNECTION INFORMATION ==="
          echo "SSH Access: ssh jacky@$(sudo tailscale ip -4)"
          echo "SSH Password: root"
          echo "Aapanel URL: https://$(sudo tailscale ip -4):7800"
          echo "Aapanel Username: jacky"
          echo "Aapanel Password: spidey"
          echo "Tailscale IP: $(sudo tailscale ip -4)"
          echo "======================================"

      # 12. Maintain Session
      - name: ⏳ Maintain VPS Session
        run: |
          echo "🖥️ VPS is now running. Create '/tmp/stop_session' to shutdown gracefully."
          while [ ! -f /tmp/stop_session ]; do
            echo "✅ Session active - Tailscale IP: $(sudo tailscale ip -4 2>/dev/null || echo 'Connecting...')"
            sleep 60
          done
          echo "Graceful shutdown initiated."

      # 13. Create Snapshot (Always Runs)
      - name: 💾 Create System Snapshot
        if: always()
        run: |
          echo "🛑 Preparing for snapshot..."
          
          # Stop services cleanly
          sudo systemctl stop apache2 || true
          sudo systemctl stop mariadb || true
          sudo systemctl stop btpanel.service || true
          sudo pkill -f "tmate" || true
          
          # Note: Don't stop Tailscale yet - we need it for upload
          echo "📸 Creating system snapshot..."
          sudo mkdir -p /mnt/snapshot
          
          # Create a sparse image file
          sudo dd if=/dev/zero of=/tmp/${SNAPSHOT_NAME} bs=1M count=1024 oflag=direct
          sudo mkfs.ext4 /tmp/${SNAPSHOT_NAME}
          
          # Mount and copy system data
          sudo mount -o loop /tmp/${SNAPSHOT_NAME} /mnt/snapshot
          
          # Copy essential system data (exclude volatile directories)
          sudo rsync -a --delete \
            --exclude=/proc \
            --exclude=/sys \
            --exclude=/dev \
            --exclude=/tmp \
            --exclude=/run \
            --exclude=/mnt \
            --exclude=/media \
            --exclude=/var/cache \
            --exclude=/var/tmp \
            --exclude=/var/lib/tailscale/tailscaled.state \
            / /mnt/snapshot/
          
          # Cleanup
          sudo umount /mnt/snapshot
          
          # Compress the snapshot
          echo "🗜️ Compressing snapshot..."
          sudo gzip /tmp/${SNAPSHOT_NAME}
          
          echo "✅ Snapshot created: /tmp/${SNAPSHOT_NAME}.gz"

      # 14. Upload Snapshot
      - name: ☁️ Upload Snapshot to MEGA
        if: always()
        run: |
          echo "🚀 Uploading snapshot to MEGA..."
          rclone copy /tmp/${SNAPSHOT_NAME}.gz ${{ env.MEGA_REMOTE }} --progress
          echo "✅ Snapshot uploaded successfully."

      # 15. Final Cleanup
      - name: 🧹 Final Cleanup & Disconnect Tailscale
        if: always()
        run: |
          echo "🔌 Disconnecting Tailscale..."
          sudo tailscale down || true
          sudo systemctl stop tailscaled || true
          
          sudo rm -f /tmp/${SNAPSHOT_NAME}* /tmp/stop_session
          echo "=== VPS Session Complete ==="
          echo "Final status: ${{ job.status }}"
          echo "Snapshot: ${MEGA_REMOTE}/${SNAPSHOT_NAME}.gz"
