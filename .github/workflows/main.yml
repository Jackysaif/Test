name: Persistent VPS with Rclone Backup

on:
  workflow_dispatch:
  workflow_call:

env:
  VPS_USER: jacky
  VPS_PASSWORD: root
  VPS_HOSTNAME: github-vps
  BACKUP_DIR: vps-backup
  SESSION_TIMEOUT: 6h

jobs:
  vps:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
      # ===== INITIALIZATION =====
      - name: Checkout
        uses: actions/checkout@v4

      # Install all necessary tools at once
      - name: Install system tools
        run: |
          sudo apt update
          sudo apt install -y curl tmate tar dpkg unzip

      # ===== RCLONE SETUP =====
      - name: Install and configure rclone
        run: |
          # Install rclone
          curl -Of https://downloads.rclone.org/rclone-current-linux-amd64.zip
          unzip -o rclone-current-linux-amd64.zip
          cd rclone-*-linux-amd64
          sudo cp rclone /usr/bin/
          sudo chown root:root /usr/bin/rclone
          sudo chmod 755 /usr/bin/rclone
          
          # Configure MEGA
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf << EOF
          [mega]
          type = mega
          user = ${{ secrets.MEGA_USER }}
          pass = ${{ secrets.MEGA_PASS }}
          EOF
          
          echo "✅ Rclone configured with MEGA"
          rclone about mega: || echo "⚠️ MEGA connection test failed"

      # ===== USER SETUP =====
      - name: Create user with sudo access
        run: |
          if ! id -u ${{ env.VPS_USER }} >/dev/null 2>&1; then
            sudo useradd -m -s /bin/bash ${{ env.VPS_USER }}
            echo "${{ env.VPS_USER }}:${{ env.VPS_PASSWORD }}" | sudo chpasswd
            sudo usermod -aG sudo ${{ env.VPS_USER }}
            echo "✅ User ${{ env.VPS_USER }} created with sudo access"
          else
            echo "🔁 User ${{ env.VPS_USER }} already exists"
          fi

      # Use the standard `hostname` command
      - name: Set hostname
        run: |
          sudo hostname "${{ env.VPS_HOSTNAME }}"
          echo "🏷️ Hostname set to $(hostname)"

      # ===== BACKUP RESTORATION =====
      - name: Restore from previous backup
        run: |
          mkdir -p restore
          
          # Download latest backup
          if rclone copy mega:${{ env.BACKUP_DIR }}/latest.tar.gz restore/ --progress; then
            echo "📦 Restoring system backup..."
            sudo tar -xzf restore/latest.tar.gz -C /
            echo "✅ System backup restored"
          else
            echo "⚠️ No system backup found, starting fresh"
          fi
          
          # Download package list
          if rclone copy mega:${{ env.BACKUP_DIR }}/packages.list restore/ --progress; then
            echo "📦 Restoring packages..."
            sudo apt-get update
            sudo dpkg --set-selections < restore/packages.list
            sudo apt-get dselect-upgrade -y
            echo "✅ Packages restored"
          fi

      # ===== TAILSCALE SETUP =====
      - name: Setup Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sudo sh
          sudo tailscaled --tun=userspace-networking &
          sleep 5
          sudo tailscale up --authkey ${{ secrets.TAILSCALE_AUTHKEY }} --hostname ${{ env.VPS_HOSTNAME }} --ssh
          
          echo "🌐 Tailscale IP: $(tailscale ip -4)"
          echo "🔑 SSH: ssh ${{ env.VPS_USER }}@$(tailscale ip -4)"

      # ===== VPS SESSION =====
      - name: Start VPS session
        run: |
          echo "🚀 VPS Session Started"
          echo "========================"
          echo "👤 User: ${{ env.VPS_USER }}"
          echo "🔑 Pass: ${{ env.VPS_PASSWORD }}"
          echo "🌐 IP: $(tailscale ip -4)"
          echo "⏰ Timeout: ${{ env.SESSION_TIMEOUT }}"
          echo "========================"
          
          # Start tmate session
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          echo "🔗 Connect: $(tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}')"
          
          # Keep session alive
          sleep 21600

      # ===== BACKUP CREATION =====
      - name: Create backup
        if: always()
        run: |
          mkdir -p backup
          echo "💾 Creating backup..."
          
          # Backup directories with their full path, including Tailscale state
          sudo tar -czf backup/latest.tar.gz \
            --ignore-failed-read \
            --warning=no-file-changed \
            /etc/ssh /etc/nginx /etc/mysql /etc/apache2 \
            /var/www /var/lib/tailscale /root/.ssh /home/${{ env.VPS_USER }} \
            2>/dev/null || true
          
          # Backup package list
          echo "📦 Backing up packages..."
          dpkg --get-selections > backup/packages.list
          
          echo "✅ Backup created: $(du -sh backup)"

      # ===== UPLOAD BACKUP =====
      - name: Upload backup to MEGA
        if: always()
        run: |
          echo "☁️ Uploading backup to MEGA..."
          rclone mkdir mega:${{ env.BACKUP_DIR }} || true
          
          # Upload final backup
          rclone copy backup/latest.tar.gz mega:${{ env.BACKUP_DIR }} --progress
          rclone copy backup/packages.list mega:${{ env.BACKUP_DIR }} --progress
          
          echo "✅ Backup uploaded to MEGA"

      # ===== FINAL REPORT =====
      - name: Session report
        if: always()
        run: |
          echo "=================================="
          echo "VPS Session Completed"
          echo "=================================="
          echo "Status: ${{ job.status }}"
          echo "Backup: ✅ Uploaded to MEGA"
          echo "Next session will restore this state"
          echo "=================================="
